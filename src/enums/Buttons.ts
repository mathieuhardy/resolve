/**
 * List of available button shapes.
 */
export enum Shape {
  Square,
  Rounded,
  Badge,
}

/**
 * Kind of the button (defines its behavior).
 */
export enum Kind {
  Clear,
  Filled,
  Toggle,
}